{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Permissions for the ADE main window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
